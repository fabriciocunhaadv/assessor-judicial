const fs = require('fs');

const original = fs.readFileSync('temp_generate_minute_original.ts', 'utf8');

// We will modify the logic directly in JavaScript/TypeScript
// I will just construct the replacement manually to ensure correctness.
